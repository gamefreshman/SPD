# 当前DPO训练策略与本轮迭代总结

## 1. 文档目的

这份文档只描述当前 Shepherd 代码里已经落地的 DPO 训练策略，以及本轮从迁移、排障到重启训练的完整结论。

它不是一份通用 DPO 教程，也不是未来实验计划。重点是：

- 当前代码到底怎么工作
- 为什么采用现在这套策略
- 这次对话里已经解决了什么问题
- 目前还剩下什么风险


## 2. 当前代码入口与关键文件

当前 DPO 训练主链路在以下文件：

- `/home1/zhh/workspace/shepherd/training/dpo_train.py`
- `/home1/zhh/workspace/shepherd/training/parameters/params_x1x3x4_dpo_finetune_nps_v2.py`
- `/home1/zhh/workspace/shepherd/src/shepherd/dpo_dataset.py`
- `/home1/zhh/workspace/shepherd/src/shepherd/dpo_utils.py`
- `/home1/zhh/workspace/shepherd/src/shepherd/lightning_module.py`
- `/home1/zhh/workspace/shepherd/training/visualize_dpo_metrics.py`

当前 `v2` 输出目录：

- `/home1/zhh/workspace/shepherd/training/jobs/x1x3x4_dpo_finetune_nps_v2`


## 3. 当前训练策略

### 3.1 DPO语义：共享 reference condition，不做 self-condition 比较

当前训练不再使用“winner 用自己的 `x3/x4`，loser 用自己的 `x3/x4`”的旧语义。

现在的语义是标准 shared-reference DPO：

- `reference` 提供固定条件
- `winner` 和 `loser` 共享同一个 reference 的 `x3/x4`
- 两者只在 `x1` 上不同

具体实现：

- `DPODataset` 持有 `reference_cache`
- 每个 pair 显式带 `reference_idx`
- `winner`/`loser` 共用同一个 timestep
- `x3` 由 `_build_x3_from_ref()` 从 reference 构造
- `x4` 由 `_build_x4_from_ref()` 从 reference 构造

对应代码：

- `/home1/zhh/workspace/shepherd/src/shepherd/dpo_dataset.py`


### 3.2 ref model 策略：固定，不做 iterative DPO

当前策略明确保持 `ref_model` 固定，不做在线更新。

原因：

- 当前 pair 数量还不够稳定
- validity 和 score gap 仍有波动
- 在这个阶段让 ref model 跟着更新，更容易把噪声写进基线

当前参数：

- `iterative_dpo_enabled = False`
- `iterative_dpo_force_update_every_n_rounds = 0`

实现上 `LightningModule` 仍然有 `ref_model`，但当前 run 中它只作为固定参考，不参与在线替换。

对应代码：

- `/home1/zhh/workspace/shepherd/src/shepherd/lightning_module.py`
- `/home1/zhh/workspace/shepherd/training/parameters/params_x1x3x4_dpo_finetune_nps_v2.py`


### 3.3 DPO loss 策略：只优化 shared-condition 下的 x1 偏好

当前要求：

- `dpo_optimize_x4 = False`

原因：

- 现在 `x4` 已经是 reference 共享条件的一部分
- DPO 训练时要比较的是 `winner/loser` 在同一条件下的 preference
- 不应该再把 `x4` 当成 winner/loser 差分通道继续优化

训练入口里有运行时保护，如果 `dpo_optimize_x4=True` 会直接报错。


### 3.4 在线采样策略

当前 `v2` 配置：

- `num_reference_molecules = 3`
- `num_samples_per_molecule = 32`
- `inference_sub_batch_size = 4`
- `dpo_sampling_every_n_epochs = 3`
- `initial_sampling_attempts = 3`

原因：

- reference 数量从最早的 `1` 提高到 `3`
- 单 reference 采样数从 `16` 提高到 `32`
- 但 `sub_batch_size` 保持 `4`，用于控制显存

当前运行方式是：

- 训练单卡
- 采样也先保守使用单卡 `sampling_gpu_ids = [0]`

虽然代码里已经支持 `MultiGPUOnlineSampler`，但当前 `v2` 不启用多卡采样，原因是前面真实服务器上已经出现过 OOM 和抢卡问题，先保稳定。

对应代码：

- `/home1/zhh/workspace/shepherd/src/shepherd/dpo_utils.py`
- `/home1/zhh/workspace/shepherd/training/parameters/params_x1x3x4_dpo_finetune_nps_v2.py`


### 3.5 打分策略

当前打分不再依赖旧版 ConfEval 的隐式行为，而是走显式的 sample -> RDKit mol -> score 流程。

#### 3.5.1 当前打分内容

当前 pair 记录中实际使用/记录的关键指标：

- `sims_surf_target`
- `sims_esp_target`
- `sims_pharm_target`
- `sa_score`
- `logp`
- `total_score`

#### 3.5.2 SA 的当前实现

当前 SA 不再是假常数 `5.0`。

现在的实现：

- 使用 RDKit Contrib 里的 `sascorer`
- 先 `Chem.RemoveHs(scoring_mol)`
- 再 `sascorer.calculateScore(...)`

这和 `shepherd-score` 里的口径已经对齐。

如果 SA 计算失败：

- `sa_score` 记为 `None`
- `sa_fallback_used = True`
- `total_score` 内部仍可暂时用 `5.0` 兜底，避免整条 score 计算直接断掉

当前 `v2` 已经验证：

- `winner SA fallback avg = 0.000`
- `loser SA fallback avg = 0.000`

说明现在 SA 不是靠 fallback 在跑。

对应代码：

- `/home1/zhh/workspace/shepherd/src/shepherd/dpo_utils.py`


### 3.6 Pair 构造策略

当前 pair builder 有两个关键约束：

- pair 比较范围是“同一个 reference 下的所有生成样本”
- 去重 key 必须带 `reference_idx`

这是前面排障中专门修过的点。

如果只在单个 inference sub-batch 内比 winner/loser，会导致：

- 同一个 reference 的 32 个样本被切碎
- 明明全局能配对，局部却经常配不出来

现在 pair key 是：

- `(reference_idx, winner_sig, loser_sig)`

这样相同分子对在不同 reference 下不会被误去重。


### 3.7 Mixed training 策略

当前训练不是纯 DPO batch，而是 mixed dataloader：

- `real_data_ratio = 0.5`

也就是标准扩散训练数据和 DPO preference batch 混合训练。

当前关键训练超参数：

- `batch_size = 2`
- `accumulate_grad_batches = 4`
- `beta_dpo = 0.3`
- `dpo_max_weight = 0.3`
- `dpo_ramp_up_epochs = 10`
- `lr = 2e-6`
- `min_lr = 2e-6`
- `lr_steps = 1`

这里有一个明确判断：

- 当前还没有证据说明应该立刻改 `ref_model`
- 也没有证据说明当前第一优先级是把 `lr/beta/weight` 大幅拉高
- 当前第一优先级仍然是稳定提供更干净的 pair 和更真实的打分


## 4. 当前 checkpoint 策略

当前已经有两层 checkpoint 机制。

### 4.1 常规 checkpoint

Lightning 仍然按 epoch 保存：

- `last.ckpt`

对应目录：

- `/home1/zhh/workspace/shepherd/training/jobs/x1x3x4_dpo_finetune_nps_v2/last.ckpt`

### 4.2 DPO 历史最佳 checkpoint

这是本轮新加的机制。

触发条件：

- 当前 round `sampling_error is None`
- `num_pairs > 0`
- `avg_score` 有效
- `avg_score` 创下历史新高

保存结果：

- 最新最佳：
  - `/home1/zhh/workspace/shepherd/training/jobs/x1x3x4_dpo_finetune_nps_v2/best_dpo_score.ckpt`
- 历史留档目录：
  - `/home1/zhh/workspace/shepherd/training/jobs/x1x3x4_dpo_finetune_nps_v2/best_dpo_score_history/`
- 保存索引：
  - `/home1/zhh/workspace/shepherd/training/jobs/x1x3x4_dpo_finetune_nps_v2/best_dpo_score_history.jsonl`

另外，这个 best 分数不会因为重启而清零。

当前实现会在 callback 初始化时读取已有的：

- `dpo_round_metrics.json`

然后把历史最高 `avg_score` 恢复回来，再继续往上比较。


## 5. 当前 metrics 与可视化策略

### 5.1 当前 round metrics 会记录什么

当前 `dpo_round_metrics.json` 会记录：

- round / epoch
- num_pairs
- validity_stats
- avg_score
- winner/loser 各项平均 score
- score_gap
- training_metrics
- `sa_fallback_used`
- `best_checkpoint_saved`
- `best_checkpoint_record`

### 5.2 当前可视化脚本做什么

当前新版 `visualize_dpo_metrics.py` 已经做了以下事情：

- 原始线保留，但淡化显示
- EMA 作为主线
- x 轴做稀疏化，避免标签挤成一团
- 引入 Shepherd baseline 对照线和区间
- 增加“Latest EMA vs Shepherd baseline”摘要面板
- 终端 summary 里直接打印当前相对 baseline 的差值

当前 baseline 的核心对照值：

- validity: `38.3%`
- surface similarity baseline mean: `0.6216`
- ESP similarity baseline mean: `0.3448`
- pharmacophore similarity baseline mean: `0.3035`
- SA baseline mean: `4.9963`
- logP baseline mean: `2.6441`
- surface upper bound mean: `0.8746`
- ESP upper bound mean: `0.8745`

Groundtruth 细节文档：

- `/home1/zhh/workspace/shepherd/training/docs/Groundtruth.md`


## 6. 当前 v2 运行状态（本轮文档写入时）

当前 active run：

- 参数文件：
  - `/home1/zhh/workspace/shepherd/training/parameters/params_x1x3x4_dpo_finetune_nps_v2.py`
- 输出目录：
  - `/home1/zhh/workspace/shepherd/training/jobs/x1x3x4_dpo_finetune_nps_v2`
- 日志：
  - 最新日志可在输出目录下查看 `nohup-*.log`

本轮重启前，`v2` 已经写出了前 4 个 round 的 metrics，历史最高：

- `best_avg_score = 5.153650337612585`

本轮重启后的 best checkpoint 逻辑会在这个历史值之上继续比较。


## 7. 本轮对话实施总结

下面是这次对话里真正落地过的关键节点。

### 7.1 迁移阶段

最开始的目标是把 SPD 里的 DPO 模块迁移到 Shepherd。

迁移后主要新增/改动过：

- `dpo_train.py`
- `dpo_dataset.py`
- `dpo_utils.py`
- `mixed_dataloader.py`
- `lightning_module.py`
- DPO 参数文件

### 7.2 语义纠偏：从 self-condition 改成 shared-reference DPO

中间最关键的一次设计澄清是：

- DPO 必须比较同一条件下的 winner/loser

所以后来把数据语义修成了：

- winner 和 loser 共用同一个 reference 的 `x3/x4`
- 不再各自用自己的条件

这是这轮里最重要的“方向修正”。

### 7.3 远端启动阶段：先解决 OOM

远端最开始多次启动失败，主要问题是：

- 多卡采样时 OOM
- 某些 GPU 本身被别的任务占用

后面的收敛策略是：

- 不再硬上多卡采样
- 先保守回到单卡训练 + 单卡采样
- `sub_batch_size = 4`

这个策略最终让训练稳定拉起来。

### 7.4 远端启动阶段：再解决 no preference pairs

OOM 之后，第二个主要问题是：

- `Initial DPO sampling produced no preference pairs`

这轮实际定位到两个根因：

1. pair builder 之前只在太小的局部范围内配对  
2. 远端 scorer / ConfEval 路径和当前 sample->mol 转换链路不一致

后面做了两件事：

- pair 构造改成按同一 reference 下的全部样本比较
- scorer 改成直接从 sample 重建 RDKit mol 做评分

之后训练才真正跨过 initial sampling。

### 7.5 SA 修复

后面又发现：

- 图里的 `SA Score` 一直平在 `5.0`

这不是“模型真的稳定在 5.0”，而是：

- SA 当时没有被正确记录
- 缺失值被硬回退成了 `5.0`

后面已经修掉：

- 改成显式 `sascorer`
- 记 `sa_fallback_used`
- 可视化脚本里把 fallback 单独汇总出来

### 7.6 Baseline 对照与结果解释

在训练能稳定运行后，又做了两件事：

- 引入 Shepherd baseline 指标
- 重写可视化脚本，让“当前 DPO vs baseline”的差距能直接看出来

这样做的目的，是避免继续只看一张没有参照物的抖动曲线。

### 7.7 历史最佳 checkpoint

最后加上的机制是：

- 每次 `avg_score` 创历史新高，额外保存 checkpoint

而且这次不是“从重启开始重新记 best”，而是：

- 先读已有 `dpo_round_metrics.json`
- 再恢复历史 best
- 然后继续比较


## 8. 当前结论

截至本轮结束，可以明确下结论的点有：

1. DPO 迁移已经不再停留在“代码可导入”，而是已经进入稳定远端训练阶段。
2. shared-reference DPO 语义已经落地，当前方向是对的。
3. ref model 当前继续固定，这是有意设计，不是遗漏。
4. SA 现在已经不是假常数，打分链路比前面真实得多。
5. baseline 对照已经接入，后续分析不需要再靠肉眼猜。
6. best checkpoint 机制已经落地，并且支持从历史 metrics 继续比较。

但同样需要保留清醒判断：

1. 当前 DPO 效果还没有强到可以下“已经显著优于 baseline”的结论。
2. `loss_dpo` 仍然偏平，说明 preference 信号还不够强。
3. 当前真正的瓶颈依然是：
   - 有效样本率
   - 可配对样本数
   - score gap 的稳定供给


## 9. 常用操作命令

查看当前 `v2` 训练进程：

```bash
ssh juwei 'ps -ef | grep -E "params_x1x3x4_dpo_finetune_nps_v2" | grep -v grep'
```

查看当前 `v2` 日志：

```bash
ssh juwei 'tail -f /home1/zhh/workspace/shepherd/training/jobs/x1x3x4_dpo_finetune_nps_v2/nohup-20260413-135944.log'
```

重新生成 `v2` 可视化：

```bash
cd /home1/zhh/workspace/shepherd/training
/home1/zhh/workspace/shepherd/.venv/bin/python visualize_dpo_metrics.py \
  /home1/zhh/workspace/shepherd/training/jobs/x1x3x4_dpo_finetune_nps_v2/dpo_round_metrics.json \
  --output /home1/zhh/workspace/shepherd/training/jobs/x1x3x4_dpo_finetune_nps_v2/dpo_round_metrics_baseline.png
```

查看 best checkpoint 目录：

```bash
ssh juwei 'ls -lah /home1/zhh/workspace/shepherd/training/jobs/x1x3x4_dpo_finetune_nps_v2/best_dpo_score*'
```
