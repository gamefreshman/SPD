# 8x4090 SPD Environment Setup Log

Date: 2026-04-02

Target server:
- Host: `qhdlink.lanyun.net`
- Port: `51255`
- User: `root`
- Target workspace: `/root/workspace/SPD`

Reference environment:
- Server alias: `juwei`
- Reference workspace: `/home1/zhh/workspace/SPD`
- Environment manager: `uv`

## 1. Initial remote machine status

Connected to the target server and confirmed:
- OS: Ubuntu 22.04.5 LTS
- GPU: `NVIDIA GeForce RTX 4090 x 8`
- Driver: `570.172.08`
- CUDA reported by `nvidia-smi`: `12.8`
- System Python present: `Python 3.10.14`
- `miniconda` existed, but this setup intentionally followed the `juwei` `uv` workflow

`nvidia-smi` showed all 8 GPUs visible and idle at the start.

## 2. Reference environment inspection on `juwei`

Checked the working `SPD` environment on `juwei` and confirmed the active project layout:
- Workspace: `/home1/zhh/workspace/SPD`
- Python pin: `3.9`
- Virtualenv path: `/home1/zhh/workspace/SPD/.venv`
- `uv` version: `0.7.19`

Read `/home1/zhh/workspace/SPD/.venv/pyvenv.cfg` and confirmed:
- interpreter source: `~/.local/share/uv/python/cpython-3.9.23-linux-x86_64-gnu/bin`
- Python version: `3.9.23`

Verified key package versions on `juwei`:
- `torch==2.5.1+cu124`
- `torchvision==0.20.1+cu124`
- `torchaudio==2.5.1+cu124`
- `torch-geometric==2.6.1`
- `torch_scatter==2.1.2+pt25cu124`
- `torch_sparse==0.6.18+pt25cu124`
- `torch_cluster==1.6.3+pt25cu124`
- `torch_spline_conv==1.2.2+pt25cu124`
- `pytorch-lightning==2.5.5`
- `rdkit==2024.09.6`
- `open3d==0.19.0`
- `e3nn==0.5.8`
- `pandas==2.2.3`
- `streamlit==1.50.0`
- `py3Dmol==2.5.4`
- `meeko==0.7.1`
- `vina==1.2.7`
- `wandb==0.22.2`

Verified `juwei` shell environment also exports:
- `PATH+=/home1/zhh/workspace/SPD/xtb-dist/bin`
- `XTBPATH=/home1/zhh/workspace/SPD/xtb-dist/share/xtb`
- `OMP_NUM_THREADS=3`
- `MKL_NUM_THREADS=3`
- `OMP_STACKSIZE=1500m`
- `ulimit -s unlimited`

## 3. Repository deployment on the 8x4090 server

The target server did not have `/root/workspace/SPD` initially.

Attempted:
- `git clone https://github.com/gamefreshman/SPD.git /root/workspace/SPD`

Result:
- clone stalled and was not suitable for continuing
- additionally, the GitHub repository content was older than the current local working tree

Resolution:
- synced the current local workspace directly from macOS to the target server with a tar stream
- excluded local-only directories such as `.git`, `.venv`, `.Project`, `.claude`, `.codebuddy`, `.trae`

After sync:
- target codebase matched the current local project structure
- verified files such as `run.sh`, `Project.md`, `src/`, `training/`, `evaluation/`, `web/`

Note:
- the tar stream created macOS metadata files like `._*`
- these were removed on the target server before environment creation

## 4. `uv` bootstrap on the 8x4090 server

Attempted the standard installer:
- `curl -LsSf https://astral.sh/uv/install.sh | sh`

Result:
- failed with TLS EOF:
  - `curl: (56) OpenSSL SSL_read: error:0A000126:SSL routines::unexpected eof while reading`

Resolution:
- copied the working `uv` and `uvx` binaries from `juwei`:
  - source: `/home1/zhh/.local/bin/uv`
  - source: `/home1/zhh/.local/bin/uvx`
  - destination: `/root/.local/bin/uv`, `/root/.local/bin/uvx`

Verified:
- `uv --version` returned `0.7.19`

## 5. `uv` mirror configuration

Created:
- `/root/.config/uv/uv.toml`

Configured default index:

```toml
[[index]]
url = "https://mirrors.cloud.tencent.com/pypi/simple/"
default = true
```

This was used to make package resolution and downloads more stable in the target network environment.

## 6. Python 3.9 environment creation

Inside `/root/workspace/SPD`:
- `uv python pin 3.9`
- `uv python install 3.9.23`
- `uv venv --python 3.9.23`

Verified:
- `.python-version` became `3.9`
- `.venv` was created successfully
- `python -V` inside the virtualenv returned `Python 3.9.23`
- interpreter path:
  - `/root/workspace/SPD/.venv/bin/python`

## 7. Core package installation

### 7.1 PyTorch CUDA stack

Installed with the PyTorch CUDA 12.4 index:

```bash
uv pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu124
```

This pulled the matching CUDA wheels and related NVIDIA packages, including:
- `nvidia-cublas-cu12`
- `nvidia-cuda-cupti-cu12`
- `nvidia-cuda-nvrtc-cu12`
- `nvidia-cuda-runtime-cu12`
- `nvidia-cudnn-cu12`
- `nvidia-cufft-cu12`
- `nvidia-curand-cu12`
- `nvidia-cusolver-cu12`
- `nvidia-cusparse-cu12`
- `nvidia-nccl-cu12`
- `nvidia-nvjitlink-cu12`
- `nvidia-nvtx-cu12`
- `triton==3.1.0`

### 7.2 PyG stack

Installed with the matching `pt25cu124` wheels:

```bash
uv pip install \
  torch-scatter==2.1.2+pt25cu124 \
  torch-sparse==0.6.18+pt25cu124 \
  torch-cluster==1.6.3+pt25cu124 \
  torch-spline-conv==1.2.2+pt25cu124 \
  torch-geometric==2.6.1 \
  -f https://data.pyg.org/whl/torch-2.5.0+cu124.html
```

### 7.3 Project runtime packages

Installed the rest of the runtime stack aligned with `juwei`:

```bash
uv pip install \
  numpy==1.26.3 \
  pandas==2.2.3 \
  pytorch-lightning==2.5.5 \
  rdkit==2024.09.6 \
  matplotlib==3.9.4 \
  jupyterlab==4.4.10 \
  wandb==0.22.2 \
  streamlit==1.50.0 \
  py3Dmol==2.5.4 \
  openpyxl==3.1.5 \
  meeko==0.7.1 \
  vina==1.2.7 \
  gemmi==0.7.5 \
  e3nn==0.5.8 \
  scikit-learn==1.6.1 \
  PyYAML==6.0.3 \
  tqdm==4.67.1
```

Installed `open3d` separately because of its wheel size:

```bash
uv pip install open3d==0.19.0
```

## 8. Editable installs

Installed the local project packages in editable mode:

```bash
uv pip install -e /root/workspace/SPD/src/score
uv pip install -e /root/workspace/SPD/src
```

Verified imports resolved to source tree paths:
- `shepherd` -> `/root/workspace/SPD/src/shepherd/__init__.py`
- `shepherd_score` -> `/root/workspace/SPD/src/score/shepherd_score/__init__.py`

## 9. `xtb` setup

Attempted direct GitHub download of `xtb` v6.7.1 tarball.

Result:
- remote download stalled and was not reliable in this environment

Resolution:
- copied the working `xtb-dist` directory from `juwei`:
  - source: `/home1/zhh/workspace/SPD/xtb-dist`
  - destination: `/root/workspace/SPD/xtb-dist`

Updated `/root/.bashrc` with:

```bash
export PATH=/root/.local/bin:$PATH
export PATH=$PATH:/root/workspace/SPD/xtb-dist/bin
export XTBPATH=/root/workspace/SPD/xtb-dist/share/xtb
export OMP_NUM_THREADS=3
export MKL_NUM_THREADS=3
export OMP_STACKSIZE=1500m
ulimit -s unlimited
```

Verified:
- `xtb --version` succeeded
- detected version: `6.7.1`

## 10. Runtime verification

Verified the following inside the target virtualenv:

```python
import torch, torch_geometric, torch_scatter, torch_sparse, torch_cluster
import pytorch_lightning, rdkit, open3d, e3nn, pandas
```

Observed versions:
- `torch 2.5.1+cu124`
- `torch_geometric 2.6.1`
- `torch_scatter 2.1.2+pt25cu124`
- `torch_sparse 0.6.18+pt25cu124`
- `torch_cluster 1.6.3+pt25cu124`
- `pytorch_lightning 2.5.5`
- `rdkit 2024.09.6`
- `open3d 0.19.0`
- `e3nn 0.5.8`
- `pandas 2.2.3`

CUDA verification:
- `torch.cuda.is_available() == True`
- `torch.cuda.device_count() == 8`

## 11. Missing resource issue discovered and fixed

When testing training imports:

```python
import new_train
import DPO1_0_triSim
```

initially failed with:

```text
FileNotFoundError: .../src/shepherd/model/equiformer_v2/nets/equiformer_v2/Jd.pt
```

This was not a Python dependency issue. The synced workspace was missing model resource files:
- `src/shepherd/model/equiformer_v2/nets/equiformer_v2/Jd.pt`
- `src/shepherd/model/equiformer_v2/ocpmodels/models/escn/Jd.pt`
- `src/shepherd/model/equiformer_v2/ocpmodels/models/scn/Jd.pt`

Resolution:
- copied the three `Jd.pt` files from `juwei` to the target server

After that:
- `import new_train`
- `import DPO1_0_triSim`

both succeeded.

## 12. Notes and caveats

### 12.1 `load_model` import

The following did not work in this current codebase:

```python
from shepherd import load_model
```

Reason:
- current `src/shepherd/__init__.py` in this workspace only exposes `__version__`
- this is a code/export issue in the repository, not an environment installation issue

### 12.2 `uv sync`

Did not use `uv sync` as the main setup path.

Reason:
- the provided workflow and prior note indicated possible ABI mismatch between `torch` and PyG wheels if `uv sync` re-resolves packages without pinning the intended CUDA wheel set

Used instead:
- `uv venv`
- explicit `uv pip install ...`

### 12.3 `run.sh`

Current `/root/workspace/SPD/run.sh` still contains:

```bash
export CUDA_VISIBLE_DEVICES="1,2"
```

This was intentionally left unchanged during environment setup.
If 8-GPU training or different GPU allocation is needed, `run.sh` should be updated separately.

## 13. Final state

As of this setup session, the 8x4090 server had:
- correct project code under `/root/workspace/SPD`
- `uv`-managed Python `3.9.23` virtualenv under `/root/workspace/SPD/.venv`
- matching core runtime versions aligned with `juwei`
- working `xtb` under `/root/workspace/SPD/xtb-dist`
- working training-script imports
- working CUDA visibility across all 8 GPUs

Recommended activation sequence on the target server:

```bash
cd /root/workspace/SPD
source .venv/bin/activate
export PATH=/root/.local/bin:$PATH
export PATH=$PATH:/root/workspace/SPD/xtb-dist/bin
export XTBPATH=/root/workspace/SPD/xtb-dist/share/xtb
```

Quick verification command:

```bash
python - <<'PY'
import torch, torch_geometric, pytorch_lightning, rdkit, open3d, e3nn
print(torch.__version__)
print(torch.cuda.is_available(), torch.cuda.device_count())
PY
```
